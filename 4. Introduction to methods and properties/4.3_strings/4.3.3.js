function truncate(str, maxlength) {
    if (str.length > maxlenght)
    {
        return (str.slice(0, maxlength - 3) + '...')

}
        return (str)

}
   alert (truncate('Васенька Васильевич Петров', 5))
